/* jshint node: true */
'use strict';

/**
  ## rtc-core/capabilities

  Provides some information as to what the current browsers capabilities are
  with regards to WebRTC.  This information is currently derived from looking
  at the browser vender and version number.
**/

var detect = require('detect');

// define some features
var features = {
  sctp: [
    'firefox|*',
    'chrome|30'
  ]
};

// iterate through the features and determine support for